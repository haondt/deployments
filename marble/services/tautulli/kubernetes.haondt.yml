components:
  primary:
    image: lscr.io/linuxserver/tautulli:latest@sha256:661569466728b2818150adef5fe0bba4db4fbfdcece8cf4b868f686f83d8cff2
    talos: +!
    networking:
      ports.http: 8181
      ingresses:
        - port: http
          host: "tautulli.{{ haondt__default_host }}"
          tls.host: "*.{{ haondt__default_host }}"
    volumes:
      data:
        src.pvc: {}
        dest.dir: /config
