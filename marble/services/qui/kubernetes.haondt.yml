components:
  primary:
    image: ghcr.io/autobrr/qui:latest@sha256:b1abd18f1d544cd5c63a3b8d9c8a91d097143e04df416567a8cb52040f380ca8
    talos: +!
    networking:
      ports:
        http: 7476
      gateway.http_routes:
        - port: http
          host: "qui.{{ haondt__default_host }}"
    volumes:
      config:
        src.pvc.size: 128Mi
        dest.dir: /config
