GOOGLE_APPLICATION_CREDENTIALS: /config/gcp_credentials.json
PersistenceSettings:
  Driver: Sqlite
  FileDataPath: /data
ScannerSettings:
  Enabled: true
  LlmProvider: OpenRouter
  OpenRouter:
    ApiKey: "{{ openrouter_api_key }}"
  OcrProvider: CloudVision
