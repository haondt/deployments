services:
  rathole-client:
    image: rapiz1/rathole
    command: --client /app/config.toml
    networks:
      - rathole
    volumes:
      - ./rathole/config.toml:/app/config.toml

networks:
  rathole:

