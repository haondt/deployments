services:
  loki:
    image: grafana/loki:latest
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - rathole
      - gabbro_grafana
    ports:
      - 3100:3100




networks:
  gabbro_grafana:
    external: true
