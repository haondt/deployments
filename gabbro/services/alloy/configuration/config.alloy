loki.write "default" {
    endpoint {
        url = "http://loki:3100/loki/api/v1/push"

    // basic_auth {
    //  username = "admin"
    //  password = "admin"
    // }
    }
}

prometheus.remote_write "mimir" {
    endpoint {
        url = "http://mimir:8080/api/v1/push"
    }
}
